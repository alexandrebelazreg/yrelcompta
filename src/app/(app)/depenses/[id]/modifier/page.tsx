import{notFound,redirect}from"next/navigation";import{ExpenseForm}from"@/components/expenses/expense-form";import{getAuthenticatedContext}from"@/lib/auth/context";import{getExpense,listSuppliers}from"@/lib/expenses/queries";
export default async function EditExpensePage({params}:{params:Promise<{id:string}>}){const{id}=await params;const{context}=await getAuthenticatedContext();if(!context.business)return null;const[e,s]=await Promise.all([getExpense(context.business.id,id),listSuppliers(context.business.id)]);if(!e)notFound();if(e.status!=="draft")redirect(`/depenses/${id}`);return <><header className="page-header"><div><p className="eyebrow">Brouillon</p><h1>Modifier {e.reference}</h1></div></header><ExpenseForm expense={e} suppliers={s}/></>}
