import Link from"next/link";import{CostingSettingsForm}from"@/components/products/costing-settings-form";import{getAuthenticatedContext}from"@/lib/auth/context";import{getCostingSettings}from"@/lib/products/queries";
export default async function CostingSettingsPage({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const q=await searchParams;const{context}=await getAuthenticatedContext();if(!context.business)return null;const settings=await getCostingSettings(context.business.id);return <><Link className="back-link" href="/produits">← Produits</Link><header className="page-header"><div><p className="eyebrow">Calcul interne</p><h1>Paramètres de coût</h1><p>Ces montants servent uniquement à estimer le coût de fabrication.</p></div></header>{q.message&&<p className="form-message success">Paramètres enregistrés.</p>}<CostingSettingsForm settings={settings}/></>}
