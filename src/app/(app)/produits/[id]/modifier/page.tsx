import Link from"next/link";import{notFound}from"next/navigation";import{ProductForm}from"@/components/products/product-form";import{getAuthenticatedContext}from"@/lib/auth/context";import{getCostingSettings,getProduct,listMaterials}from"@/lib/products/queries";
export default async function EditProductPage({params}:{params:Promise<{id:string}>}){const{id}=await params;const{context}=await getAuthenticatedContext();if(!context.business)return null;const[product,materials,settings]=await Promise.all([getProduct(context.business.id,id),listMaterials(context.business.id),getCostingSettings(context.business.id)]);if(!product)notFound();return <><Link className="back-link" href={`/produits/${id}`}>← Produit</Link><header className="page-header"><div><p className="eyebrow">Catalogue</p><h1>Modifier {product.name}</h1></div></header><ProductForm product={product} materials={materials} settings={settings}/></>}
