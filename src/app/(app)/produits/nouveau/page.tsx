import Link from"next/link";import{ProductForm}from"@/components/products/product-form";import{getAuthenticatedContext}from"@/lib/auth/context";import{getCostingSettings,listMaterials}from"@/lib/products/queries";
export default async function NewProductPage(){const{context}=await getAuthenticatedContext();if(!context.business)return null;const[materials,settings]=await Promise.all([listMaterials(context.business.id),getCostingSettings(context.business.id)]);return <><Link className="back-link" href="/produits">← Produits</Link><header className="page-header"><div><p className="eyebrow">Catalogue</p><h1>Nouveau produit</h1></div></header><ProductForm materials={materials} settings={settings}/></>}
